[project]
name = "px4-ulog-plottools"
version = "0.1.0"
description = "Project-local Python environment for PX4_ulog_plottools (only provides the pyulog CLI tools: ulog2csv, ulog_info, ulog_messages, ulog_params)."
requires-python = ">=3.9"
dependencies = [
    "pyulog",
]
